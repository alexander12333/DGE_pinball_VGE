#config_version=6

config:
  - config_ball_devices.yaml
  - config_coils.yaml
  - config_switches.yaml

modes:
  - attract
  - base

playfields:
  playfield:
    default_source_device: bd_trough
    tags: default